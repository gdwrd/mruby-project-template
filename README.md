# MRuby Project template
This is simple project template for [MRuby](https://github.com/mruby/mruby)

## Usage
Add your Ruby code to `lib.rb` file and `lib/` folder.
Add mruby gems to `mruby_config.rb`

## Build
Run `rake build`

## Run
Run `rake run` or `./bin`
